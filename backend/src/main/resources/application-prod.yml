spring:
  datasource:
    url: jdbc:postgresql://${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DB}
    username: ${POSTGRES_USER}
    password: ${POSTGRES_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        default_schema: ${POSTGRES_SCHEMA}

springdoc:
  api-docs:
    enabled: false
  swagger-ui:
    enabled: false

app:
  api:
    base-url: ${APP_API_BASE_URL:http://localhost:8080}
  jwt:
    secret: ${JWT_SECRET}
    issuer: ${JWT_ISSUER}
    access-ttl-minutes: ${JWT_ACCESS_TTL_MINUTES}
    refresh-ttl-days: ${JWT_REFRESH_TTL_DAYS}
  admin:
    bootstrap-secret: ${ADMIN_BOOTSTRAP_SECRET}
  media:
    storage-dir: ${MEDIA_STORAGE_PATH}
    base-url: ${MEDIA_BASE_URL:${APP_API_BASE_URL:http://localhost:8080}/media}
  zyra:
    provider: ${ZYRA_PROVIDER:OPENAI}
    base-url: ${ZYRA_BASE_URL:https://api.openai.com/v1}
    model: ${ZYRA_MODEL:gpt5}
    api-key: ${OPENAI_API_KEY}
    max-history: ${ZYRA_MAX_HISTORY:10}
