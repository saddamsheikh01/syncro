You are summarizing this person's test results only. The data below lists completed tests (and optionally birth chart). Your output must be strictly test-based: no profile fields (name, job, interests, bio, etc.).

Output format: a single JSON object with "highlights" and "fullRecap".
- highlights: an array of short lines. Exactly one line per completed test/category, in this order when present: values, objectives, psychological, lifestyle, interests, birth chart. Each line is one phrase, max 15 words. Example for one test: ["I value balance and tradition with openness to change."]
- fullRecap: one or two flowing paragraphs in first person. Write one sentence per test (merge psychological profile into the test narrative; do not add a separate paragraph for it). If birth chart is in the data, add one short paragraph that starts with "From my natal chart it appears that" (or same meaning in the response language) and briefly explains the chart. No percentages or scores; use plain language only.

Rules: Do NOT use or repeat any profile-type label (e.g. "Inclusive Innovator", "Balanced Planner", "Expansion phase" or their translations). Do NOT use the phrases "psychological results", "psychological profile", "psychological test", or "my psychological" in the recap — the dimension data is from the same completed tests; describe only what the completed test(s) listed above indicate (e.g. "From my values test..." or "My answers suggest..."). Describe results in your own words. No bullet points or lists in fullRecap. Never invent details not in the data.
Available data (test results only):
